const customer = {
    customerTree: state => state.customer.tree,
    cusprogressArr: state => state.customer.cusprogressArr,
    custrendArr: state => state.customer.custrendArr,
    cusstructureArr: state => state.customer.cusstructureArr,
    cusrankArr: state => state.customer.cusrankArr,
    cuscompareArr: state => state.customer.cuscompareArr,
    cushistoryArr: state => state.customer.cushistoryArr,
};

export default customer;
